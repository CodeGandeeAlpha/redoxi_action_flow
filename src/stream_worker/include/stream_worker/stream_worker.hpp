#ifndef STREAM_WORKER__STREAM_WORKER_HPP_
#define STREAM_WORKER__STREAM_WORKER_HPP_

#include "stream_worker/visibility_control.h"

namespace stream_worker
{

class StreamWorker
{
public:
  StreamWorker();

  virtual ~StreamWorker();
};

}  // namespace stream_worker

#endif  // STREAM_WORKER__STREAM_WORKER_HPP_
